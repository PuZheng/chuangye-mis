import x from '../xx.js';

export const invoiceTypes = x([]).setTag('invoice-types');
export const loading = x(0).setTag('loading');
export const invoice = x({}).setTag('invoice');
export const vendors = x([]).setTag('vendors');
export const purchasers = x([]).setTag('purchasers');
export const accountTerms = x([]).setTag('account-terms');
