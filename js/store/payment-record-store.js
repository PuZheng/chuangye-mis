// import request from '../request';

export default {
  save(/*records*/) {
    // TODO unimplemented
    return new Promise(function (resolve) {
      setTimeout(function () {
        resolve({});
      }, 1000);
    });
  }
};
